# Copy to `.env` and fill in. docker compose reads this automatically.

# Hugging Face token with access to the (gated) model repo.
# Create one at https://huggingface.co/settings/tokens (read scope is enough).
HF_TOKEN=

# Optional overrides:
# VPI_MODEL_ID=Ngseo/hyundai-uiwang-left-flowmatch
# VPI_DEVICE=cuda
